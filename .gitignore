.git/
node_modules/

# Salida transitoria de Playwright (se regenera en cada corrida local
# o de CI — .github/workflows/tests-visuales.yml ya la sube como
# artifact de Actions; nunca debe vivir en el repo ni deployarse).
/playwright-report/
/test-results/

.wrangler/
*.patch
revision-ingenieria-*.md

# Salida transitoria de Lighthouse (ver verificar-lighthouse.yml, se sube
# como artifact de Actions; no debe vivir en el repo)
/lighthouse-reports/
